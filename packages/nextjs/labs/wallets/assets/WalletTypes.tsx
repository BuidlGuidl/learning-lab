"use client";

import { makeIllustration } from "./Illustration";

export const WalletTypes = makeIllustration({
  src: "/cold-vs-hot-wallets.webp",
  width: 1200,
  height: 675,
  alt: "A side-by-side comparison of wallet types: a cold hardware wallet keeps keys secured offline, while a hot software wallet stays connected on a phone and laptop for convenient transactions.",
});
