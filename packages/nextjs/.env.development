# Local dev only: the postgres from `docker compose up -d` at the repo root. Committed on purpose,
# so nothing secret goes here — real keys (OpenRouter, Better Auth in PR 1) live in .env.local.
DATABASE_URL="postgresql://postgres:mysecretpassword@localhost:5432/postgres"
